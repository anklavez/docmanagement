version: '2'
services:
    docmanagement-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb docmanagement-mongodb-config
    docmanagement-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    docmanagement-mongodb-config:
        image: mongo:3.6.3
        container_name: docmanagement-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
